<html>
    <head>
        <link rel="stylesheet" href="/public/css/success.css" type="text/css" />
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css" type="text/css"/>
    </head>
    <body>
        <% if (data) { %>
            <div class = "container">
                <div class="box">
                    <h1>Success</h1>
                    <h3>Your short 🔗 has been created successfully 😁</h3>
                    <span id="slug" style="display:none"><%= data.slug %></span>
                    <div class = "linkbox">
                        <input id="short-link" value="<%= data.slug %>" readonly/>
                        <button onclick="goToLink()"><i class="fas fa-link"></i></button>
                        <button onclick="copyToClipboard()"><i class="fas fa-copy"></i></button>
                    </div>
                </div>
            </div>
        <% } %>
    </body>
    <script src="/public/js/success.js"></script>
</html>